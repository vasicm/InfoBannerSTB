#ifndef HEADER_LISTS
#define HEADER_LISTS

typedef enum {

}REMOTE_CODE;

 void initEventListener();
 REMOTE_CODE getCode();
// int32_t getKeys(int32_t count, uint8_t* buf, int32_t* eventsRead);

#endif